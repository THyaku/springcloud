server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848 #配置Nacos地址
        server-addr: 175.24.121.219:1111 #配置Nacos地址

# 配置刷新暴露
management:
  endpoints:
    web:
      exposure:
        include: '*'
